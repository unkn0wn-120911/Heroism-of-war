MONGODB_URI="mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority"
SERVER_PORT=9000
GAME_MODE="battle_royale"

# Real secret values should stay in a local .env file and must not be committed.
